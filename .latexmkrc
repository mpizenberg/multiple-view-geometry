# Set output directory
$out_dir = 'build';

# Set pdf output
$pdf_mode = 1;

# Set pdf viewer as in 'start VIEWER'
$pdf_previewer = 'start mupdf';

# Continuous mode of pdf viewer
$preview_continuous_mode = 1;

# How to make the PDF viewer update its display when the PDF file changes.
# See the man page for a description of each method.
$pdf_update_method = 2;
